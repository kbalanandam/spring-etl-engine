targets:
  - format: relational
    targetName: CustomersSql
    schema: dbo
    table: Customers
    writeMode: insert
    batchSize: 100
    connectionRef: sqlserver-main
    fields:
      - name: id
        type: int
      - name: name
        type: String
      - name: email
        type: String


