/**
 * Deprecated legacy validation-config model and loader package retained only for cleanup and migration.
 *
 * <p>This package is not part of the active ETL runtime path. Active validation configuration now
 * flows through source and processor config models loaded by {@code com.etl.config.ConfigLoader}.</p>
 */
package com.etl.validation.config;

