# Config category: Runtime

Future landing zone for runtime-selection and execution-time config docs (for example selected-job entry and runtime-level overrides).

No existing pages are moved here yet.

